/**
 * Fonts are linked into the native projects so `DancingScript-Regular` is
 * available by name on both platforms. Run `npx react-native-asset` after
 * changing anything in assets/fonts.
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts'],
};
